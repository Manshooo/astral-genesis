# Состояние проекта

Снимок того, что реально реализовано в **Astral Genesis** на текущий момент
(после `alpha-v0.2.1`). Акцент — на архитектуре и потоках данных между
системами и на честном перечне пробелов. Дополняет, а не дублирует:
[[История]] (лор), how-to ([[Взаимодействие]], [[Цикл забега]]) и `CLAUDE.md`
(архитектурные инварианты в корне репозитория).

> Игра — FP пост-апокалиптический рогалик на **Godot 4.7** (Forward+, Jolt).
> Игрок — БФЖ, бестелесное сознание; ядро — **захват тела** врагов.

---

## 1. Архитектура: как всё связано

Игра целиком на **GECS** (ECS-аддон, сабмодуль). Кадровый цикл крутится
**вручную** в [main.gd](../../src/world/main.gd):

| Godot-хук | Группа ECS | Что внутри |
|---|---|---|
| `_process` | `input` | `S_PlayerInput`, `S_InteractInput` |
| `_process` | `gameplay` | `S_FPSLook`, `S_Health`, `S_Lifespan` |
| `_physics_process` | `physics` | `S_PlayerMovement`, `S_Movement`, `S_InteractionDetector`, `S_BodySnatch` |
| — | наблюдатели | `O_OutlineVisual`, `O_ExpelFromBody`, `O_ApplySkillEffects` |

Системы, кастующие лучи (детект интеракций, захват тела), живут в группе
`physics` — Jolt на отдельном потоке, space-state запросы безопасны только из
`_physics_process`.

### Автолоады (точки координации)

- **ECS** — мир GECS.
- **RunManager** — владелец одного забега: граф + активная комната + переходы
  (см. [[Цикл забега]]).
- **WorldSave** — `world_seed` + `death_count`, детерминированная генерация.
- **SkillManager** — дерево навыков, очки, ранги; сигнал `skill_unlocked`.
- **SettingsManager** — настройки (мышь, движение, FOV, FPS), рантайм-эффекты.
- **UIManager** — единый стек экранов (пауза/настройки/дерево навыков); Esc
  всегда закрывает верхний.
- **GameConfig** — `RS_GameConfig` (в т.ч. библиотека пресетов комнат).

### Три ключевых потока

1. **Забег:** меню → `WorldSave.new_game` → `RunManager.enter_complex` →
   `RS_LevelGraph.generate_run` → спавн комнат → выход/смерть. Детали и схема —
   [[Цикл забега]].
2. **Взаимодействие:** `S_InteractionDetector` каждый физкадр кастует луч из
   камеры, вешает/снимает `C_Highlighted` (→ прицел растёт, `O_OutlineVisual`
   рисует обводку); `S_InteractInput` по «interact» зовёт `interact()` цели.
   Детали — [[Взаимодействие]].
3. **Захват тела (ядро):** ЛКМ ставит `C_BodySnatch.capture_requested` →
   `S_BodySnatch` кастует луч по слою `enemies`, бросает
   `capture_success_chance`, при успехе «вселяет» душу в тело. См. §3.

---

## 2. Что реализовано

### Ядро и управление — ✅ работает
- FP-передвижение (`S_PlayerInput` → `S_PlayerMovement`/`S_Movement`,
  `C_Velocity`), обзор мышью (`S_FPSLook`), прыжок.
- Единственная управляемая `E_Player` («душа-риг»): все системы управления
  кастуют её `as E_Player` (жёсткая связанность — осознанный архитектурный
  выбор: захват = обмен данных на одном риге, а не переселение управления).
- Настройки (чувствительность, скорость, FOV, FPS) с персистом и применением
  на лету.

### Взаимодействие — ✅ работает
- `C_Interactable` + слой `interactives` + `interact()`/`actions[]`.
- Подсветка через `C_Highlighted`, обводка `O_OutlineVisual`, растущий прицел.
- Действия-ресурсы: `A_OpenSkillTree`, `A_TravelThroughDoor`, `A_FinishRun`.

### Генерация мира и забег — ✅ в основном
- `RS_LevelGraph.generate_run` — весь граф комплекса детерминированно
  (слои по глубине, этажи, остовное дерево + доп. рёбра, вертикальные
  коннекторы, тупик-дом на L3, выходы на поверхности).
- `RunManager` спавнит/деспавнит одну активную комнату, телепортирует игрока,
  штампует `C_DoorPortal` на двери по рёбрам узла графа.
- Победа через комнату-выход (`A_FinishRun`).
- Есть верификатор генерации `dev/gen_verifier.tscn`.

### Захват тела и жизненный цикл БФЖ — ✅ петля замкнута (полный разбор: [[Захват тела]])
- `S_BodySnatch` — луч/бросок/вселение/поглощение тела, события
  `body_snatched` / `body_snatch_failed`.
- `S_Health` — clamp HP, единичное объявление смерти (`C_Dead` + `entity_died`).
- `S_Lifespan` — тик распада в развоплощённом состоянии, `run_ended` при нуле.
- `O_ExpelFromBody` — гибель захваченного тела выбрасывает БФЖ в призрака
  (отложенно, чтобы не залипнуть в `C_Dead`) — забег **не** завершается.
- `O_RunEnded` — распад призрака (`run_ended`) → `RunManager.die()`:
  `record_death()` (`death_count++`), снос забега, экран смерти. Настоящая смерть.
- **Цель захвата есть:** `E_Body` (`src/entities/body/e_body.tscn`) — инертное
  тело/труп с `C_Health` + `C_BodySnatchable` на слое `enemies`.
- Всё **зарегистрировано** в `world.tscn`.

**Чего не хватает:** у тел нет ИИ (статичны) — [[Враг-тело]]; визуал тела не
переносится при вселении и прицел не реагирует на цель захвата — см. пробелы ниже.

### Навыки — ✅ бэкенд, ⚠️ контент
- `SkillManager`: ранги, стоимость, требования (ранг навыка / сумма по ветке),
  персист.
- `O_ApplySkillEffects` применяет эффекты навыков к `E_Player` по событию
  `skill_unlocked` (реально тюнит `capture_range`, `max_duration`).
- Дерево открывается интеракцией с инкубатором в хабе; UI — `SkillTreeUI`.
- Пробел: очки навыков сейчас неоткуда получать в геймплее (нет источника),
  контента навыков мало — см. [[v0.3.0]].

### UI — ✅ работает
- HUD с прицелом, стек экранов (`UIManager`), пауза, настройки, дерево навыков,
  главное меню (3D-фон).

---

## 3. Модель захвата тела (кратко)

Модель — «душа-риг + состояние воплощения»:

| Состояние | Компоненты на `E_Player` | Поведение |
|---|---|---|
| **Воплощён** | `C_Embodied` + `C_Health` | здоровье тела; распад на паузе |
| **Развоплощён** | нет `C_Embodied`/`C_Health` | призрак; тикает `C_Lifespan`; истечение = смерть забега |

Захват = обмен данных на одном риге (не переселение управления между
сущностями). `C_BodySnatch` + `C_Lifespan` живут на игроке через
`E_Player.define_components()`.

---

## 4. Известные пробелы

Отсортировано по влиянию на играбельность.

- **У тел нет ИИ.** `E_Body` статичен (труп/манекен) — захват работает, но живой
  цели нет. → [[Враг-тело]], отложено в [[v0.4.0]].
- **Визуал тела не переносится при вселении** — риг остаётся белой капсулой
  игрока вместо визуала захваченного тела. → [[Визуал захваченного тела]].
- **Прицел не реагирует на цель захвата** — нет непрерывной детекции тела и
  вида-крестика. → [[Прицел — крестик при захвате тела]].
- **«Загрузить игру» не реализована.** Кнопка «Загрузить» в меню есть, но
  пустая — сигнал `pressed` не подключён. `WorldSave` умеет восстанавливать
  сохранение, но UI-входа для загрузки нет.
- **Замки/ключи-заглушки.** `A_TravelThroughDoor` при запертой двери печатает в
  консоль; инвентаря ключей нет (`locked_by` только штампуется). → [[v0.4.0]]
  «Переработать Level Access Key».
- **Вертикальные порталы не подключены.** `A_UsePortal.execute` — пустой `pass`.
- **Нет подсказок к интерактивным объектам** (`prompt_text` в `C_Interactable`
  есть, но не отображается). → [[v0.3.0]].
- **Пресеты комнат — бутстрап.** Сопоставление слот↔ребро по порядку («Вариант
  A»); умное сопоставление (пресет объявляет свои слоты) и типы комнат A/C —
  впереди, см. [[v0.4.0]].
- **Экрана победы нет** — `finish_run()` уводит в меню-заглушку.
- **Баг тачпада** (нельзя вращать головой с тачпада) — [[Фикс тачпада (если возможен)]].

## 5. Планы (ближайшие вехи)

- **[[v0.3.0]]:** настройки управления, подсказки к интерактивам, больше навыков
  и источник очков, спавн игрока после генерации, [[Враг-тело]].
- **[[v0.4.0]]:** комнаты типов A и C, переработка Level Access Key, онбординг,
  карта.
- **[[Бэклог]]:** визуальные эффекты при открытии навыка.
- Диаграммы взаимодействия систем (задача документации из [[v0.3.0]]).

---

## Карта соответствия: код ↔ роль

| Слой | Где | Примеры |
|---|---|---|
| Компоненты (данные) | `src/components/` | `C_Health`, `C_BodySnatch`, `C_DoorPortal`, `C_Highlighted` |
| Системы (поведение) | `src/systems/` | `S_BodySnatch`, `S_Lifespan`, `S_InteractionDetector` |
| Сущности | `src/entities/` | `E_Player`, `E_Incubator`, `E_ComplexDoor` |
| Наблюдатели | `src/observers/` | `O_ExpelFromBody`, `O_OutlineVisual` |
| Действия | `resources/interaction/`, entity-папки | `A_TravelThroughDoor`, `A_FinishRun` |
| Ресурсы-данные | `src/resources/`, `data/` | `RS_LevelGraph`, `RS_SkillTree`, `RS_Settings` |
