Добавлять игрока на уровень уже после генерации мира
Учесть WorldSave и новую игру

---

**Сделано.** `RunManager.enter_complex()` теперь спавнит душу скриптом:
`_spawn_player()` инстанцирует `PLAYER_SCENE` (`e_player.tscn`) и регистрирует в
мире **до** генерации графа; позиционирует её `_place_player_in_room` при спавне
входной комнаты. WorldSave/новая игра учтены: сид забега выведен из
(`world_seed`, `death_count`), а свежая `E_Player` несёт непочатый `C_Lifespan`
(идентичность через `define_components()`). Осталось прогнать тестами.