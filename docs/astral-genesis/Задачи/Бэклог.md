# Не забыть

> Разобран при планировании v0.6.0: всё, что было готово к постановке, разошлось
> по доскам (помечено ниже). Здесь остаётся то, что ещё не дозрело до версии.

- [ ] На подумать: **анимированный пролёт спиной по комнатам** обратно в лабораторию при распаде БФЖ. Первоначальная идея карточки, ставшей [[Экран итогов забега]]; в v0.6.0 вместо неё взяты затемнение и сводка забега. Чтобы вернуться к пролёту, нужен лог маршрута (в сейве лежит `visited_node_ids` **без порядка**) либо догрузка чужих слоёв — гранула стриминга это слой; и надо помнить, что после смерти `death_count++` меняет сид, то есть везут игрока по комплексу, которого через секунду не будет
- [x] Заметка-кандидат (геймплей, на будущее): поведение таймера распада `C_Lifespan` во плоти. Сейчас во плоти он замирает (пауза). Варианты: (а) оставить паузу; (б) медленно капает и во плоти — давление никогда не снимается полностью; (в) во плоти медленно восстанавливается к максимуму. Правится в `S_Lifespan` (сейчас `with_none([C_Embodied])`).
      Теперь медленно убывает во плоти
- [x] Визуальные эффекты при открытии нового скилла (после изучения в Skill Tree) → **[[v0.5.0]]**, к теме скиллов
- [x] Вертикальное перемещение между комнатами — сделать его только порталом, а не порталом и дверями. С появлением карты стало видно, что часть дверей по факту замещает вертикальный переход: у комнаты один портал, а вертикальных рёбер может быть несколько, и лишние падают обычным дверям (`RunManager._bind_portals`). На карте такая дверь выглядит как соседняя комната этажа, хотя ведёт на другой слой. Слишком неочевидно, надо менять. (Варианты пока не прорабатывались.) → **[[v0.5.0]]**
- [x] [[Обводка силуэтом через постэффект]] — ровная толщина и один силуэт на составной объект вместо `material_overlay` на каждом меше; заодно уходит мерцание из [[Взаимодействие]] §5 → **[[v0.6.0]]**
- [x] [[Единый редактор геймдизайна]] — собрать «Шаблоны», «Генератор» и будущий редактор Skill Tree в одно окно с вкладками → **[[v0.6.0]]**, вместе с [[Улучшить редактор генератора]] (тот переехал туда же, чтобы не верстать дважды)
- [x] Также, стоит подправить генератор. Сейчас иногда можно встретить узлы, в которых двери "заварены" (без level_access_key, насколько я понял). Нужно сделать так, чтобы таких узлов было как можно меньше и выбор падал в пользу более подходящих узлов по количеству дверей → **[[v0.6.0]]**
- [ ] [[Осколки Кавума]] — очки навыков как лут по комнатам вместо/вдобавок к наградам за прогресс забега. Отделено от [[Скиллы]] (v0.5.0): это кусок левел-дизайна (пикап, слот в пресетах комнат), а не просто экономика, и пересекается с валютой [[Артефакт «Архитектор»]] — решать вместе с ней, не раньше
- [ ] На подумать: **скилл, который сам пишет описание релиза** — разбирает диффы влитых PR версии и собирает их в читаемое «что нового» вместо голого номера версии (`gh release create --generate-notes` сейчас просто перечисляет заголовки PR). Живой прогон v0.5.0 показал разрыв: в этой версии главным была расширенная система скиллов (десять навыков вместо двух, экономика очков), плюс улучшения UI и базовый ИИ врага — в описании релиза не видно ничего из этого. Кандидат встроить в `release.yml` (шаг «Опубликовать релиз») или сделать отдельным ручным шагом перед публикацией — не решено
- [ ] На подумать: **топики версии — отдельными PR в версионную ветку, а не коммитами напрямую**. Ветка `release/vX.Y.Z` остаётся версионной (как сейчас), но каждая тема/фича идёт своим PR в неё, а не пишется прямо на неё коммитами подряд — так проще собирать и просматривать, что вошло в версию, прежде чем сама версионная ветка идёт финальным PR в master. Пока не опробовано на практике — сырая идея с прошедшего v0.5.0, где всё легло напрямую на `release/v0.5.0`
- [ ] [[Система боя]] способности разных тел, урон, баланс. Бой за БФЖ при получении нужного навыка
