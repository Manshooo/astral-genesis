---

kanban-plugin: board

---

## К выполнению



## В работе



## Тестирование



## Готово

**Выполнено**
- [x] Подсказки к интерактивным объектам — Label в HUD по `C_Highlighted` показывает `[F] prompt_text`; `hud_prompt.gd`
- [x] [[Враг-тело]]
- [x] [[Фикс тачпада (если возможен)]] — не баг движка (OS-level libinput DWT); задокументирован workaround в карточке
- [x] Флоу смерти — распад в развоплощён (`run_ended`) → `WorldSave.record_death()` → экран смерти (`O_RunEnded` → `RunManager.die`)
- [x] HUD витали — таймер распада (`C_Lifespan`) всегда, HP тела (`C_Health`) только во плоти (`C_Embodied`); `hud_vitals.gd`
- [x] [[Добавить спаун игрока после генерации мира в скрипте]]
- [x] Справка управления — экран/.txt со списком раскладки (WASD / Space / мышь / F / ЛКМ — захват / Esc)


***

## Архивировать

- [x] Документация. Создать диаграммы взаимодействия систем

%% kanban:settings
```
{"kanban-plugin":"board","list-collapse":[false,false,false,false]}
```
%%