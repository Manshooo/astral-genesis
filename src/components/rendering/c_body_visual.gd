class_name C_BodyVisual
extends Component
## Визуал захваченного тела, надетый на душу-риг. Запрос к презентации: логика
## (S_BodySnatch) вешает компонент на игрока, O_BodyVisual его применяет,
## O_ExpelFromBody снимает — и наблюдатель возвращает прежний вид.
##
## Наличие компонента = «риг сейчас выглядит как чужое тело». Модель захвата —
## «душа-риг + обмен данных»: сущность тела поглощается, поэтому её меш надо
## перенести на риг, а не переносить сам узел.

## Меш и переопределение материала, снятые с захваченного тела.
@export var mesh: Mesh
@export var material_override: Material

## Трансформ меша относительно КОРНЯ тела — он же «относительно подошвы»: origin
## сцены тела стоит в ступнях. Едет вместе с мешем, потому что у рига origin в
## ЦЕНТРЕ его коллайдера: оставить облику трансформ меша рига — и тело повиснет
## на полроста над полом. Заодно переносится масштаб и сдвиг, заданные мешу в сцене
## тела: это тоже часть облика, а не случайность авторинга.
@export var mesh_transform: Transform3D

## Насколько origin рига окажется ВЫШЕ подошвы, когда этот облик будет надет, —
## то же число, что C_BodyForm.foot_offset(), и снятое с той же формы тела.
##
## Едет вместе с обликом, а не спрашивается у рига в момент применения: облик и
## габарит — РАЗНЫЕ компоненты с разными наблюдателями, и порядок их вызова
## задаёт очередь команд, а не мы. Спросить E_Player.foot_offset() значило бы
## получить габарит, который риг ещё НОСИТ: призрачный при первом захвате,
## прошлого тела — при пересадке.
##
## ZERO — тело габарита не даёт: риг остаётся в своей форме, и офсет считает он
## сам (ровно та же развилка, что в C_BodyForm.foot_offset).
@export var foot_offset: Vector3

## Что вернуть при развоплощении, включая трансформ — надевая облик, мы его
## перезаписываем. Заполняет O_BodyVisual в момент применения: авторский вид БФЖ
## живёт в e_player.tscn, и больше его взять неоткуда.
@export var restore_mesh: Mesh
@export var restore_material: Material
@export var restore_transform: Transform3D
